{% extends "MyAppProjetBundle::layout.html.twig" %}


{% block content %}
    <h1>Favoris</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Idclient</th>
                <td>{{ entity.idClient }}</td>
            </tr>
            <tr>
                <th>Idoffre</th>
                <td>{{ entity.idOffre }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('favoris') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('favoris_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
